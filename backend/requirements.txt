Flask==3.0.2
flask-cors==3.0.10  # Ensure compatibility with Flask 3.0.2
pypdf==4.1.0
groq  # Specify version if known, or use latest compatible version
gunicorn==20.1.0  # Add gunicorn version for server deployment
requests==2.31.0
